server:
  error:
    include-stacktrace: never

spring:
  dataSource:
    url: jdbc:mysql://localhost:3306/springbootessentials?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

